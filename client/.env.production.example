# # 生产环境 API 地址（构建时注入，必须与后端实际地址一致）
# 部署时：复制为本文件去掉 .example 后的名称，即 .env.production
#   cp .env.production.example .env.production
# 然后执行 npm run build（会读取 .env.production）
#
# 部署到 172.16.80.132 时使用：
REACT_APP_API_URL=http://172.16.80.132:3011/api

# 若通过 Nginx 反向代理后端，可设为相对路径（与前端同域）：
# REACT_APP_API_URL=/api
